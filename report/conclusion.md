# Conclusion

> Template.

- **Restate** the problem and that the ARE solves it via runtime, per-request
  strategy selection.
- **Summarize findings** — adaptive selection outperforms fixed strategies on
  mixed workloads; quantify with your headline numbers.
- **Contributions** — the engine, the pluggable architecture, the zero-budget
  Docker evaluation method.
- **Limitations** — simulated network/edge, rule-based (not learned) decisions,
  synthetic pages.
- **Future work** — see `docs/future-work.md` (framework plugin, real edge, ML).
